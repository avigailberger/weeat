<%= form_with(model: restaurant, local: true) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <p id="notice"><%= notice %></p>
  <div class="field">
    <%= form.label :restaurant_name %>
    <%= form.text_field :restaurant_name, id: :restaurant_name %>
  </div>

  <div class="field">
    <%= form.label :cuisine_name %>
    <%= form.collection_select :cuisine_id, @cuisines, :id, :descriptionE, include_blank: true %>
  </div>

    <div class="field">
      <%= form.check_box :is_ten_bis, id: :restaurant_is_ten_bis %>
      <%= form.label :is_ten_bis %>
    </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :restaurant_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
