<!--&lt;!&ndash;<p id="notice"><%= notice %></p>

<h1>Restaurants</h1>

<table>
  <thead>
    <tr>
      <th>Restaurant Name</th>
      <th>Cuisine</th>
      <th>Is Ten Bis:</th>
      <th>Rating:</th>
      <th>Address</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= restaurant.restaurant_name %></td>
        <td><%= restaurant.cuisine %></td>
        <td><%= restaurant.is_ten_bis %></td>
        <td><%= restaurant.rating %></td>
        <td><%= restaurant.address %></td>
        <td><%= link_to 'Show', restaurant %></td>
        <td><%= link_to 'Edit', edit_restaurant_path(restaurant) %></td>
        <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'New Restaurant', new_restaurant_path %>&ndash;&gt;-->

<!DOCTYPE html>
<html>
<head>
  <title>WeEat</title>
  <%= javascript_pack_tag 'restaurants_react' %>
</head>

<body>
<%= yield %>
</body>
</html>