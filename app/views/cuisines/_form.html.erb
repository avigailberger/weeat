<%= form_with(model: cuisine, local: true) do |form| %>
  <% if cuisine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cuisine.errors.count, "error") %> prohibited this cuisine from being saved:</h2>

      <ul>
      <% cuisine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descriptionE, 'Description English' %>
    <%= form.text_area :descriptionE, id: :cuisine_descriptionE %>
  </div>

  <div class="field">
    <%= form.label :descriptionH,'Description Hebrew' %>
    <%= form.text_area :descriptionH, id: :cuisine_descriptionH %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_area :icon, id: :cuisine_icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
